<% if target.errors.any? %>
  <div class="error_explanation">
    <% unless defined?(skip_header) %>
      <h2><%= pluralize(target.errors.count, "error") %> prevented this record from being saved:</h2>
    <% end %>
    <ul>
      <% target.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
